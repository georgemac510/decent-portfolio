import '../styles/App.module.css'; // Adjust the path according to your file structure

export default function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}

// export default MyApp
