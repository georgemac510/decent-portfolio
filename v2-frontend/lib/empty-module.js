// Stub for Node-only modules


const noop = () => () => ({
    // Minimal libp2p component shape

  });
  
  // Default export for `import x from '...'`
  export default {};
  
  // Named exports for `import { webRTC, webRTCDirect } from '@libp2p/webrtc'`
  export const webRTC = noop;
  export const webRTCDirect = noop;